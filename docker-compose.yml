services:
  sqlserver:
    image: mcr.microsoft.com/mssql/server:2022-latest
    environment:
      MSSQL_SA_PASSWORD: "SuaSenhaForte123!"
      ACCEPT_EULA: "Y"
    ports:
      - "1433:1433"

  api:
    build: .
    ports:
      - "8080:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ConnectionStrings__DefaultConnection=Server=sqlserver;Database=GamingJourneyDB;User Id=sa;Password=SuaSenhaForte123!;TrustServerCertificate=True;
      - Jwt__Key=gaming-journey-chave-secreta-minimo-32-caracteres-aqui
    depends_on:
      - sqlserver